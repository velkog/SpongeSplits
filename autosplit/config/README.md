Here is where the autosplit configuration goes. 

Edit the config-sample.yaml file, and save it as config.yaml

The structure might look a bit ugly for now, but I wanted to keep it flexible in case other split cases are added. The next logical addition would be adding socks. But loading screens, text bubbles, etc are all possible additions. 